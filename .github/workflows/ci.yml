name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  openapi-sdk-freshness:
    name: OpenAPI → SDK freshness check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: pnpm/action-setup@v3
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile
      - name: Check SDK is up to date with OpenAPI
        run: bash packages/sdk/scripts/check-sdk-freshness.sh

  sdk-only-enforcement:
    name: SDK-only enforcement (no raw fetch/axios in Next apps)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Scan operator app for raw fetch/axios
        run: |
          if rg -n --glob '*.{ts,tsx}' "fetch\\(|from ['\\\"]axios['\\\"]|require\\(['\\\"]axios['\\\"]\\)" apps/operator/src | rg -v "getApiClient|api-client|node_modules"; then
            echo "FAIL: raw fetch()/axios found in operator app"
            exit 1
          fi
          echo "PASS: no raw fetch/axios in operator app"
      - name: Scan admin app for raw fetch/axios
        run: |
          if rg -n --glob '*.{ts,tsx}' "fetch\\(|from ['\\\"]axios['\\\"]|require\\(['\\\"]axios['\\\"]\\)" apps/admin/src | rg -v "getApiClient|api-client|node_modules"; then
            echo "FAIL: raw fetch()/axios found in admin app"
            exit 1
          fi
          echo "PASS: no raw fetch/axios in admin app"

  no-prisma-in-frontend:
    name: No Prisma imports in Next apps
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check no Prisma in operator or admin
        run: |
          if grep -rn "@prisma/client\|from 'prisma'" apps/operator/src apps/admin/src 2>/dev/null; then
            echo "FAIL: Prisma import found in frontend"
            exit 1
          fi
          echo "PASS: no Prisma in frontends"

  ui-color-lint:
    name: UI Color Lint (no hard-coded hex)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - uses: pnpm/action-setup@v3
        with:
          version: 9
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Run UI color lint
        run: pnpm ui:color-lint

  api-unit-tests:
    name: API Unit Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - uses: pnpm/action-setup@v3
        with:
          version: 9
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Run unit tests
        run: pnpm --filter @vexel/api test -- --passWithNoTests --testPathPattern="spec.ts$"

  e2e:
    name: Playwright E2E Tests
    runs-on: ubuntu-latest
    needs: [api-unit-tests]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - uses: pnpm/action-setup@v3
        with:
          version: 9
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Copy env file
        run: cp .env.example .env && sed -i 's/REPLACE_WITH_64_BYTE_HEX_SECRET/ci-test-jwt-secret-not-for-production-use-only/' .env
      - name: Start Docker stack
        run: docker compose up -d --build
        env:
          JWT_SECRET: ci-test-jwt-secret-not-for-production-use-only
          NEXT_PUBLIC_API_URL: http://127.0.0.1:9021
          TENANCY_DEV_HEADER_ENABLED: "true"
      - name: Show API logs on failure
        if: failure()
        run: docker compose logs api --tail=100 || true
      - name: Wait for API health
        run: |
          for i in $(seq 1 30); do
            if curl -fsS http://127.0.0.1:9021/api/health > /dev/null 2>&1; then
              echo "API is healthy"
              break
            fi
            echo "Waiting for API... ($i/30)"
            sleep 5
          done
      - name: Run database migrations and seed
        run: |
          docker compose exec -T api npx prisma migrate deploy
          docker compose exec -T api npx ts-node --transpile-only --skip-project --compiler-options '{"module":"NodeNext","moduleResolution":"NodeNext"}' prisma/seed.ts
      - name: Install Playwright browsers
        run: pnpm --filter @vexel/e2e exec playwright install --with-deps chromium
      - name: Run E2E tests
        run: pnpm --filter @vexel/e2e test
        env:
          CI: true
          API_BASE: http://127.0.0.1:9021
          OPERATOR_BASE: http://127.0.0.1:9024
          ADMIN_BASE: http://127.0.0.1:9023
      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: apps/e2e/playwright-report/
          retention-days: 7
      - name: Tear down Docker stack
        if: always()
        run: docker compose down -v
