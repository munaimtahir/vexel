# Route and Endpoint Classification

This report classifies all route paths:

- **MVP Active (LIMS)**: `/lims/*` paths (registrations, encounters, sample collection, results, verification, reports), `/health`, `/health/worker`, `/health/pdf`, `/health/deep`, `/system-logs`, `/auth/login`, `/auth/refresh`, `/auth/logout`, `/me`.
- **Future / Non-MVP**: All `/opd/*` and `/rims/*` routes.
- **Internal / System**: `/jobs/*`, `/ops/*`.
- **Orphans**: None detected.
- **Needs Fix**: None.
