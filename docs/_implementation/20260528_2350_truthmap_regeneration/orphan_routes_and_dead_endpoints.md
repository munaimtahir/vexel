# Orphan Routes and Dead Endpoints

| Endpoint | Status | Notes |
| -------- | ------ | ----- |
| `POST /encounters/{id}:verify` | DEPRECATED | Use `POST /verification/encounters/{id}:verify`. |
| `GET /api/docs` | DEV_ONLY | Swagger UI. |
